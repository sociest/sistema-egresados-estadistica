## Roadmap Actualizado Final — Sistema Seguimiento Egresados UMSA

---

### 🔴 BLOQUE 0 — Limpieza y reestructura de la BD (PRIMERO, bloquea todo lo demás)

**Qué hay que hacer:**

Este bloque hay que hacerlo antes que cualquier otro porque todos los demás dependen del schema.

**Cambios en `schema.ts`:**
- Tabla `egresado`: eliminar `apellidos`, `direccion`, `planEstudiosNombre`, `idPlan`, `fechaTitulacion`, `fechaGraduacion`; cambiar `ciudadResidencia` + `regionResidencia` → un solo campo `lugarResidencia`; cambiar `planeaTitularse` de boolean a enum `("Si", "No", "No sabe")`; el campo `tituloAcademico` pasa a ser calculado (no se guarda en BD, se deriva del postgrado más alto — Doctorado > Maestría > Especialidad > Diplomado > Licenciatura/sin título)
- Tabla `historialLaboral`: renombrar `ciudad` → `ciudadRegionTrabajo`; renombrar `sector` → `sectorTrabajo`; eliminar todos los campos de verificación de documentos (`documentoBinario`, `documentoNombre`, `documentoTipo`, `documentoSubidoEn`, `verificacionEstado`, `verificadoEn`, `rechazoMotivo`)
- Tabla `postgrado`: eliminar `esSolicitudCambio`, `datosPropuestos`, y todos los campos de verificación de documentos
- Modificar `semestreIngreso` y `semestreEgreso` de `smallint` a `varchar(10)` para soportar formato texto `"I/2020"` o `"II/2020"`

**Migración SQL:** generar script `migration_v8.sql` con todos los `ALTER TABLE DROP COLUMN`, `ALTER TABLE RENAME COLUMN`, `ALTER TABLE ALTER COLUMN TYPE`, y los nuevos enums sin perder datos existentes.

**Archivos a modificar:** `src/lib/schema.ts`, `src/lib/validations.ts`, `scripts/seed.ts`, nuevo `migration_v8.sql`

---

### 🔴 BLOQUE 1 — Limpiar código post-migración BD

**Qué hay que hacer:**

Con la BD reestructurada, limpiar todo el código que referencia los campos eliminados.

**Archivos y carpetas a eliminar completamente:**
- `src/app/verificaciones/page.tsx`
- `src/components/verificaciones/VerificacionesClient.tsx`
- `src/components/verificaciones/VerificacionesPostgradoClient.tsx`
- `src/app/api/historial/[id]/verificar/route.ts`
- `src/app/api/postgrado/[id]/verificar/route.ts`
- `src/app/api/postgrado/pendientes/route.ts`

**Formularios y componentes a limpiar:**
- `EgresadoForm.tsx`: quitar `apellidos`, `direccion`, `planEstudiosNombre`, unificar campos de residencia en `lugarResidencia`, cambiar `planeaTitularse` a tres opciones (Sí / No / No sabe), quitar `tituloAcademico` del formulario (ahora es calculado)
- `HistorialForm.tsx`: renombrar `ciudad` → `ciudadRegionTrabajo`, quitar sección de subida de documento, quitar estado de verificación
- `PostgradoForm.tsx`: quitar sección de documento, quitar lógica de solicitud de cambio y estado de verificación, simplificar
- `RegistroInicialForm.tsx`: quitar campos eliminados, agregar `lugarResidencia`
- `BuscadorEgresados.tsx`: quitar filtro de plan de estudios, agregar filtro de modalidad
- `AdminLayout.tsx`: quitar enlace a Verificaciones del sidebar

**APIs a limpiar:**
- `src/app/api/egresados/route.ts`: quitar referencias a campos eliminados
- `src/app/api/egresados/[id]/route.ts`: ídem
- `src/app/api/historial/route.ts`: renombrar campo, quitar documento
- `src/app/api/historial/[id]/route.ts`: ídem
- `src/app/api/postgrado/route.ts`: quitar documento y campos eliminados
- `src/app/api/postgrado/[id]/route.ts`: simplificar
- `src/app/api/egresados/importar/route.ts`: actualizar para nuevo schema

**Archivos de vistas a limpiar:**
- `src/app/egresados/[id]/page.tsx`: quitar referencias a campos eliminados, agregar cálculo de título académico
- `src/app/(egresado)/mi-perfil/page.tsx`: quitar campos eliminados
- `src/app/(egresado)/editar-perfil/page.tsx`: ídem

---

### 🔴 BLOQUE 2 — Correcciones de bugs en CRUD de egresados

**Qué hay que hacer:**

- Corregir que todos los egresados aparecen "sin empleo" a pesar de tener historial laboral activo — revisar y corregir el subquery en el SELECT de `egresados/page.tsx` y en `src/app/api/egresados/route.ts`
- En la columna "Titulación" del listado: mostrar `anioTitulacion` si existe, o `"--"` si no tiene
- Agregar columna separada "Egreso" que muestre `anioEgreso` de cada egresado
- En la columna "Postgrado": corregir el subquery que verifica si tiene postgrados — mostrar el tipo más alto que tenga (Doctorado, Maestría, etc.) o `"--"` si no tiene ninguno
- Quitar columna "Plan de estudios" del listado (ya no existe en BD)
- Agregar columna "Modalidad" en el listado
- Verificar y corregir que al crear un egresado desde el panel admin se genere automáticamente el usuario con CI como contraseña inicial — el bloque de creación de usuario en `POST /api/egresados` está incompleto actualmente

**Archivos a modificar:** `src/app/egresados/page.tsx`, `src/app/api/egresados/route.ts`, `src/components/egresados/BuscadorEgresados.tsx`

---

### 🔴 BLOQUE 3 — Arreglar vista de detalle del egresado (admin)

**Qué hay que hacer:**

- Corregir colores de texto en la vista de detalle `/egresados/[id]/page.tsx` — varios textos no se ven por bajo contraste (textos `text-slate-500` sobre fondos oscuros, etc.)
- En la sección de historial laboral: hacer cada entrada expandible con botón "Ver detalle" que muestre todos los campos incluyendo `ciudadRegionTrabajo`, `sectorTrabajo`, tipo de contrato y área
- En la sección de postgrados: expandir cada entrada para mostrar todos los campos con más espacio, o agregar botón "Ver más" que los muestre en un panel más grande
- El `tituloAcademico` ya no viene de BD — calcularlo en la página derivando del postgrado más alto registrado (orden: Postdoctorado > Doctorado > Maestría > Especialidad > Diplomado; si es Titulado sin postgrados: "Lic. en Estadística"; si es Egresado sin postgrados: sin título)
- Quitar todos los campos que ya no existen en la BD de esta vista

**Archivos a modificar:** `src/app/egresados/[id]/page.tsx`

---

### 🟡 BLOQUE 4 — Landing page con KPIs reales

**Qué hay que hacer:**

- Crear endpoint público (sin autenticación) `GET /api/stats/publicos` que consulte la BD y devuelva: total de titulados registrados, total de egresados registrados, tasa de empleabilidad de titulados (%), tiempo promedio de inserción laboral en meses
- Reemplazar los 4 cuadrados de estadísticas estáticas en `LandingLoginPage.tsx` con datos reales cargados desde este endpoint al montar el componente con `useEffect`
- Mostrar un estado de carga (skeleton) mientras llegan los datos y un fallback al valor estático si falla la petición

**Archivos a crear/modificar:** nuevo `src/app/api/stats/publicos/route.ts`, `src/app/login/LandingLoginPage.tsx`

---

### 🟡 BLOQUE 5 — Importación Excel mejorada

**Qué hay que hacer:**

Reestructurar completamente la lógica de importación para que coincida con el nuevo formato requerido.

**Columnas del nuevo Excel de importación:**
- Nombres, Apellido Paterno, Apellido Materno, CI, Género
- Semestre de Ingreso (formato `I/2020` o `II/2020`)
- Semestre de Egreso (formato `I/2020` o `II/2020`)
- ¿Es titulado? (Si / No)
- Solo si es titulado: Año de titulación, Modalidad (Tesis / Proyecto de grado / Examen de grado / Otro), Área de especialización

**Lógica de parseo:** extraer `anioIngreso` y `semestreIngreso` del campo `"I/2020"` → año 2020, semestre 1. Si es titulado, validar que tenga año de titulación y modalidad. Derivar el campo `tipo` (Titulado/Egresado) de la columna ¿Es titulado?

**Actualizar la plantilla descargable** para que refleje exactamente el nuevo formato con los encabezados correctos y una fila de ejemplo de titulado y una de egresado.

**Archivos a modificar:** `src/app/api/egresados/importar/route.ts`, `src/components/egresados/ImportarEgresadosBtn.tsx`

---

### 🟡 BLOQUE 6 — Reportes mejorados

**Qué hay que hacer:**

- Agregar a los filtros de `ReportesClient.tsx` los mismos filtros avanzados que tiene el CRUD de egresados: tipo (Titulado/Egresado), modalidad de titulación, sector laboral, ciudad/región de trabajo, con/sin postgrado
- Actualizar las columnas de la exportación Excel: quitar columna "Plan de estudios", agregar "Lugar de residencia", "Semestre ingreso", "Semestre egreso", "Área de especialización", asegurarse que "Modalidad" usa los valores nuevos incluyendo "Examen de grado"
- Verificar que todos los gráficos existentes funcionen correctamente con los datos del nuevo schema
- Pasar los nuevos filtros al endpoint `GET /api/reportes` y aplicarlos en los queries

**Archivos a modificar:** `src/components/reportes/ReportesClient.tsx`, `src/app/api/reportes/route.ts`

---

### 🟡 BLOQUE 7 — Imágenes de noticias en el servidor (Opción A — almacenamiento local)

**Qué hay que hacer:**

- Crear la carpeta `public/uploads/noticias/` en el proyecto (agregar `.gitkeep` para que git la incluya pero ignorar los archivos de imagen con `.gitignore`)
- Crear endpoint `POST /api/upload` que reciba un archivo de imagen via `FormData`, valide que sea JPG/PNG/WEBP y menor a 5MB, genere un nombre único con timestamp para evitar colisiones (`noticia_1234567890_nombre.jpg`), lo guarde en `public/uploads/noticias/` usando `fs.writeFile`, y devuelva la URL pública `/uploads/noticias/nombre.jpg`
- Modificar `NoticiaForm.tsx` para que el campo de imagen tenga dos pestañas: "Subir imagen" (input file con preview) y "Pegar URL" (input texto como ahora) — ambas guardan el valor en el mismo campo `imagenUrl`
- En `docker-compose.yml` montar el directorio como volumen persistente para que las imágenes sobrevivan reinicios del contenedor: `./public/uploads:/app/public/uploads`
- En `nginx.conf` configurar cache para archivos estáticos de `/uploads/` con headers apropiados

**Archivos a crear/modificar:** nuevo `src/app/api/upload/route.ts`, `src/components/noticias/NoticiaForm.tsx`, `public/uploads/noticias/.gitkeep`, actualizar `.gitignore`

---

### 🟡 BLOQUE 8 — Encuesta (configurable desde BD)

**Qué hay que hacer:**

- Nueva tabla `encuestas`: id, titulo, descripcion, activa (boolean, solo una activa a la vez), fechaCreacion
- Nueva tabla `preguntas_encuesta`: id, idEncuesta, texto, tipo enum (`texto_libre`, `opcion_multiple`, `escala_1_5`), orden, opciones (JSON para opción múltiple)
- Nueva tabla `respuestas_encuesta`: id, idEgresado, idEncuesta, idPregunta, respuesta (texto), creadoEn — con restricción única (idEgresado, idPregunta) para evitar duplicados
- En `mi-perfil/page.tsx`: agregar banner/botón "Responder encuesta" que aparezca si hay una encuesta activa y el egresado no la ha respondido aún; por ahora al hacer click muestra modal con "Encuesta próximamente disponible — las preguntas serán configuradas por el administrador"
- CRUD admin en `/encuestas-admin/page.tsx` para crear encuestas, agregar/reordenar preguntas, ver respuestas
- Vista de respuesta de encuesta desde el perfil del egresado como página o modal
- Agregar "Encuestas" al sidebar de AdminLayout

**Archivos a crear:** actualizar `src/lib/schema.ts`, nuevos `src/app/api/encuestas/route.ts`, `src/app/api/encuestas/[id]/route.ts`, `src/app/api/encuestas/[id]/responder/route.ts`, `src/app/encuestas-admin/page.tsx`, `src/components/perfil/EncuestaModal.tsx`

---

### 🟠 BLOQUE 9 — Audit log y backup a Google Drive

**Qué hay que hacer:**

**Audit log:**
- Nueva tabla `audit_log`: id, idUsuario, accion enum (`crear`, `editar`, `eliminar`), entidad enum (`egresado`, `usuario`, `noticia`, `encuesta`), entidadId, datosAnteriores (JSON texto), datosNuevos (JSON texto), ip, creadoEn
- Nuevo `src/lib/audit.ts` con función `registrarAudit(opts)` que inserta en la tabla — debe ser llamada desde los route handlers de forma no bloqueante (no usar await, solo fire-and-forget para no enlentecer las respuestas)
- Agregar llamadas en: `POST /api/egresados`, `PUT /api/egresados/[id]`, `DELETE /api/egresados/[id]`, `POST /api/usuarios`, `PUT /api/usuarios/[id]`, `DELETE /api/usuarios/[id]`
- Vista admin `/actividad/page.tsx` con tabla paginada (20 por página), filtros por entidad, acción y rango de fechas
- Agregar "Actividad" al sidebar de AdminLayout

**Backup a Google Drive:**
- Script `scripts/backup.sh` que ejecuta `pg_dump` dentro del contenedor Docker, comprime con `gzip` (nombre: `backup_YYYYMMDD_HHMMSS.sql.gz`), sube el archivo a Google Drive usando `rclone` (herramienta gratuita de CLI), y elimina backups locales de más de 7 días
- Script de configuración `scripts/setup-rclone.sh` con instrucciones paso a paso para autorizar `rclone` con Google Drive (requiere hacer el proceso una sola vez en el servidor con un navegador o token)
- Crontab entry para ejecutar el backup todos los días a las 2:00am: `0 2 * * * /ruta/al/proyecto/scripts/backup.sh >> /var/log/backup-egresados.log 2>&1`
- Endpoint admin `GET /api/backup` que genere y descargue un Excel completo con: hoja Egresados (todos los campos), hoja Historial Laboral, hoja Postgrados, hoja Usuarios (sin passwordHash), hoja Audit Log (últimos 1000 registros) — útil para backup manual inmediato sin acceso al servidor

**Archivos a crear/modificar:** actualizar `src/lib/schema.ts`, nuevo `src/lib/audit.ts`, actualizar routes de egresados y usuarios, nuevo `src/app/actividad/page.tsx`, nuevo `src/app/api/backup/route.ts`, nuevos `scripts/backup.sh` y `scripts/setup-rclone.sh`, actualizar `src/components/shared/AdminLayout.tsx`

---

### 🔵 BLOQUE 10 — Docker y despliegue en servidor

**Qué hay que hacer:**

- `Dockerfile` multi-stage: stage 1 instala dependencias y compila el proyecto Next.js, stage 2 copia solo lo necesario para producción — resultado: imagen liviana de ~300MB
- `docker-compose.yml` con tres servicios: `db` (PostgreSQL 16 con volumen persistente `./data/postgres`), `app` (Next.js en puerto 3000, depende de `db`), `nginx` (reverse proxy en puertos 80 y 443, redirige a `app:3000`)
- `nginx.conf` con: redirect de HTTP a HTTPS, reverse proxy a Next.js, headers de seguridad (HSTS, X-Frame-Options, etc.), cache de archivos estáticos de `/uploads/` (1 mes), límite de tamaño de subida (`client_max_body_size 10m`)
- `.env.production.example` con todas las variables: `DATABASE_URL`, `JWT_SECRET`, `NODE_ENV=production`, `NEXTAUTH_URL`, y en el futuro `TWILIO_*` para SMS
- `scripts/deploy.sh` para actualizar la aplicación en producción: `git pull`, `docker-compose build app`, `docker-compose up -d app` (sin bajar la BD)
- `scripts/restore-backup.sh` para restaurar desde un archivo `.sql.gz`: descomprime y carga con `pg_restore` dentro del contenedor de la BD
- `README-deploy.md` con guía completa paso a paso: requisitos del servidor (Ubuntu 22.04+, 2GB RAM mínimo, 20GB disco), instalación de Docker, clonar el repo, configurar `.env.production`, primer despliegue, cómo actualizar, cómo restaurar backup, cómo ver logs

**Archivos a crear:** `Dockerfile`, `docker-compose.yml`, `nginx.conf`, `.env.production.example`, `scripts/deploy.sh`, `scripts/restore-backup.sh`, `README-deploy.md`

---

## 📋 Orden recomendado de implementación

Los bloques 0 y 1 son bloqueantes — hay que hacerlos primero y en ese orden. Los bloques 2 y 3 son correcciones de bugs visibles para el cliente, conviene hacerlos antes de una demo. Los bloques 4 al 7 son mejoras de funcionalidad. Los bloques 8 y 9 son los dos que faltaban del roadmap anterior. El bloque 10 es el de infraestructura y se puede hacer en cualquier momento pero es necesario antes del despliegue final.

¿Arrancamos con el Bloque 0?